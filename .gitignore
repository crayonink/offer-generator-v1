# Node (Antora docs toolchain) — local only; Railway must NOT see these or
# nixpacks treats the app as Node and skips the Python (requirements.txt) build.
node_modules/
package.json
package-lock.json
# Antora default build dir (we publish to ./docs_site instead)
build/

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.venv/
venv/
.env/

# Streamlit
.streamlit/

# OS
.DS_Store
Thumbs.db

# Excel / Word temp files
~$*.xlsx
~$*.docx

# IDE
.vscode/
.idea/

# Secrets
client_secret.json
token.pickle

# Generated / ephemeral (not needed in repo)
uploads/
quotes/
export/
.claude/
seed_price_master.py

# Keep vlph.db tracked so Railway has the seeded price data
