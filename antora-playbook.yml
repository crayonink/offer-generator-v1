site:
  title: Offer Generator Documentation
  # Published under the FastAPI app at automation.encon.co.in/docs
  url: /docs
  start_page: offer-generator::index.adoc
content:
  sources:
  - url: .
    branches: HEAD
ui:
  bundle:
    url: https://gitlab.com/antora/antora-ui-default/-/jobs/artifacts/master/raw/build/ui-bundle.zip?job=bundle-stable
    snapshot: true
output:
  # Built site is committed and served by the FastAPI app at /docs
  # (Railway builds Python-only, so the docs are pre-built locally).
  dir: ./docs_site